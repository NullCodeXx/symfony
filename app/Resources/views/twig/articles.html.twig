{% for article in articles %}
    {{ article.title }}
    {{ article.content }}
    {{ article.createdAt|date('d-m-Y') }}
    {{ article.updatedAt|date('d-m-Y') }}
    {{ article.isEnabled }}
    {{ article.nbLike }}
{% endfor %}